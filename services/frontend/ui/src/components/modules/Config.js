export const moduleConfigurations = {
  Dimensioning: {
    requiredState: ['simulation', 'barnStockItems'], // Définir les clés de données requises
  },
  // Ajoutez d'autres modules avec leurs clés de données requises ici
}
